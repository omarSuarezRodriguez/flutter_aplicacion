import 'package:flutter/material.dart';

class App_7 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('App 7', style: TextStyle(fontSize: 40.0)),
    );
  }
}