import 'package:flutter/material.dart';

class App_6 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('App 6', style: TextStyle(fontSize: 40.0)),
    );
  }
}